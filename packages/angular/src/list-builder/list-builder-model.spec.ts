import { AIListBuilderModel } from './list-builder-model.class';

describe('List builder model', () => {
  it('should initialize an AIListBuilderModel', () => {
    const model = new AIListBuilderModel();
  });

});
